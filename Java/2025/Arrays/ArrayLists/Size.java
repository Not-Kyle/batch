import java.util.ArrayList;

public class Main {
    public static void main(String[] args) {

        ArrayList<String> network = new ArrayList<>();
        network.add("[ERROR]");
        network.add("[ERROR]");
        network.add("[ERROR]");

        int sizeOf = network.size();
        System.out.println(sizeOf);
    }
}
